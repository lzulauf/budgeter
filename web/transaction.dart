class Transaction {
  String id;
  String date;
  String payee;
  String description;
  double outflow;
  double inflow;
  
  Transaction(this.id, this.date, this.payee, this.description, this.outflow, this.inflow);
}